# CodeFRAME — Agent Guidelines

**The guidelines for this repo live in [`CLAUDE.md`](CLAUDE.md). Read that file.**

It is the single source of truth for product vision, the primary contract, architecture
rules, repository structure, commands, and working mode — for every coding agent, not
just Claude.

This file used to duplicate that content and drifted out of sync (it described a
`server/` directory that no longer exists and a phase two releases old). Rather than
re-sync two copies, `CLAUDE.md` is now the only copy. This pointer stays because
`AGENTS.md` is a conventional entry point that some agents look for first.
